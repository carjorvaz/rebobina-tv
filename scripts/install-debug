#!/usr/bin/env bash
set -euo pipefail

repo_root="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"

"${repo_root}/scripts/build-debug"
exec adb install -r "${repo_root}/app/build/outputs/apk/debug/app-debug.apk"
